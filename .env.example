# Docker Compose: copy this file to .env and set your admin password (and optional SECRET_KEY).
#   cp .env.example .env
# Then run: docker compose up --build
# If you don't create .env, default admin password is "admin".

ADMIN_PASSWORD=admin
SECRET_KEY=change-me-use-openssl-rand-hex-32
