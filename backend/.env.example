# Database
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/hamachletz

# CORS (comma-separated or single origin)
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

ACCESS_TOKEN_EXPIRE_MINUTES=1440
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_WHATSAPP_FROM=
TWILIO_SMS_FROM=

# For Docker: admin password and SECRET_KEY are set in the project root .env (see root .env.example).
# For local runs: add ADMIN_PASSWORD and SECRET_KEY here (required).
# DEBUG=false
